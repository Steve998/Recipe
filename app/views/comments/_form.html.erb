<%= form_for([@recipe, @comment], :html => {:multipart => true} ) do |f| %>

  <div class="field">
  <h3><%= f.label :description %></h3>
      <%= f.text_area :description, :size => "143%x12%" %>
  </div>


  <div class="actions">
      <%= f.submit %>
  </div>

<% end %>
